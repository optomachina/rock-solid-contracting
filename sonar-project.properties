sonar.projectKey=optomachina_rock-solid-contracting
sonar.organization=optomachina

sonar.sources=src
sonar.exclusions=**/node_modules/**,**/.next/**,**/public/**

# Services.tsx contains a data array of six service entries with a uniform
# shape (title/description/features/image/position). The repetition is
# intentional — it's configuration data, not duplicated logic — so it's
# excluded from copy-paste detection.
sonar.cpd.exclusions=src/components/Services.tsx
